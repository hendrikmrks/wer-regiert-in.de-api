{
  "name": "wer-regiert-in.de-api",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "build": "mkdir -p dist && cp -r app.js package.json package-lock.json node_modules dist/ && cp -r public dist/ 2>/dev/null || true"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
